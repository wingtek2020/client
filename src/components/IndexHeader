import { Button, Card, CardBody, Container, Row, Col } from "reactstrap";

const IndexHeader = () => {
    return (
        <>
        <div>
            Hello World
        </div>
        </>
    );
};

export default IndexHeader;